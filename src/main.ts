// Placeholder entry point. The real UI (search, tree, buy list) lands in a later phase — see SPEC.md.
// The engine in ./engine.ts is complete, pure and DOM-free; this file only proves the build wires up.
export {};
